package common;

public enum QueryType {
    // Connection types
    CONNECTION, CONNECTION_ACCEPTED, CONNECTION_REJECTED,
    // Search types
    SEARCH, SEARCH_RESPONSE,
    // Download content types
    DOWNLOAD,
}
